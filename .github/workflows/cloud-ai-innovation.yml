name: Cloud AI Innovation

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write
  actions: read

jobs:
  innovate-and-advertise:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install deps (frozen)
        run: npm ci

      - name: Generate Cloud AI innovations
        run: node automation/cloud-ai-innovation-factory.cjs

      - name: Update indexes and advertise
        run: |
          node automation/docs-pages-indexer.cjs
          node automation/front-index-directory-builder.cjs
          node automation/cloud-ai-innovation-advertiser.cjs

      - name: Commit changes
        run: |
          git config user.name "zion.app bot"
          git config user.email "bot@users.noreply.github.com"
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "chore(automation): cloud AI innovation content [skip ci]"
          fi

      - name: Push
        if: success()
        run: git push


