name: CI/CD Pipeline

on:
  push:
    branches: [ main, gh-pages ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Add your build/deploy steps here"
