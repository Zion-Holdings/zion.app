name: Auto Label PRs

permissions:
  contents: read
  pull-requests: write

on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: auto-label-${{ github.ref }}
  cancel-in-progress: true

jobs:
  label:
    name: Apply Labels
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Label PR
        uses: actions/labeler@v7 # v6.0.1
        with:
          repo-token: ${{ github.token }}
          configuration-path: .github/labeler.yml
          sync-labels: true
