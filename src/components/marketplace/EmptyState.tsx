

  Wifi,;''';
  ShoppingCart,;''';
} from '@/components/ui/icons;'';

import { useTranslation } from 'react-i18next;'';
export interface EmptyStateProps {;''';
    | 'products;''';
    | 'categories;''';
    | 'talent;''';
    | 'equipment;''';
    | 'search;''';
    | 'error;''';
    | 'network;''';
    | 'loading;'';
  title?: string;''';
      "We'';
      "We'';
      "We'';
      "We'';
      "We'';
      "We'';
      ? { label: 'Try Again'';
    ? "We'';
    : "We'';
        onRetry ? { label: 'Refresh Categories'';
        onRetry ? { label: 'Reset Filters'';
        onRetry ? { label: 'Refresh Listings'';
      action={onRetry ? { label: 'Clear Search'';
      action={onRetry ? { label: 'Try Again'';
      action={onRetry ? { label: 'Retry'';
      ? { label: 'Try Again'';
    ? "We'';
    : "We'';
        onRetry ? { label: 'Refresh Categories'';
        onRetry ? { label: 'Reset Filters'';
        onRetry ? { label: 'Refresh Listings'';
      action={onRetry ? { label: 'Clear Search'';
      action={onRetry ? { label: 'Try Again'';
      action={onRetry ? { label: 'Retry''