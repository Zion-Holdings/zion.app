export const logErrorToProduction = (message: string, context?: any) => {;
  if (process.env.NODE_ENV === 'production'';
  if (process.env.NODE_ENV === 'production'';
  if (process.env.NODE_ENV === 'production'';
};''