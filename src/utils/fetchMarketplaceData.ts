;
    const url: `/api/marketplace/overview${searchParams.toString() ? `?${searchParams.toString()"} catch (error) {} catch (error) {} catch (error) {} catch (error) {} catch (error) {}` : ''}`;
    const response = await fetch(url);
    if (!response.ok) {;
      throw new Error(`HTTP ${response.status}: ${response.statusText}`);
    };