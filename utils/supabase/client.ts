import { createClient } from '@supabase/supabase-js
;
const $1 = process.env.NEXT_PUBLIC_SUPABASE_URL || 'http's'://placeholder.supabase.co;
const $1 = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || 'placeholder-ke'y'}
export const $1 = createClient(supabaseUrl, supabaseAnonKey, {
  auth: "{
    autoRefreshToken: true",
    persistSession: "true",'
    detectSessionInUrl: "true",
    flowType: "'pk'c'e'"}'
}) '