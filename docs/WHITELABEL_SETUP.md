# White‑Label Multitenancy Setup

- Create tables/policies:
  - Run `scripts/supabase/multitenancy.sql` and `scripts/supabase/requests.sql` in Supabase SQL editor
  - Create public storage bucket `assets`
- Env vars:
  - NEXT_PUBLIC_SUPABASE_URL
  - NEXT_PUBLIC_SUPABASE_ANON_KEY
  - SUPABASE_SERVICE_ROLE_KEY
  - APEX_DOMAIN (e.g. `ziontechmarketplace.com`)
- DNS/SSL:
  - Provision wildcard SSL for `*.ziontechmarketplace.com`
  - Tenants with custom domains must CNAME to your apex/app domain; ensure SSL on edge or platform
- Admin:
  - Admin UI at `/admin/whitelabel`
  - Create/edit/suspend/reset tenants
- Partner panel:
  - `/partner` (content, users, analytics, AI tools)
- Theming:
  - Theme presets: light, dark, neon, corporate, startup
  - Navbar/footer/hero text editable per tenant (APIs TBD)