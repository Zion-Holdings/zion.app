# Configuration Examples - Smart Predictive Analytics Solution

## Basic Configuration

```json
{
  "service": {
    "name": "Smart Predictive Analytics Solution",
    "version": "1.0.0",
    "port": 3000
  },
  "database": {
    "url": "postgresql://localhost:5432/predictive-analytics-meca0rw1",
    "pool": {
      "min": 2,
      "max": 10
    }
  },
  "api": {
    "rateLimit": 100,
    "timeout": 30000
  }
}
```

## Docker Compose

```yaml
version: '3.8'
services:
  predictive-analytics-meca0rw1:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: predictive-analytics-meca0rw1
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password

  redis:
    image: redis:7-alpine
```

## Kubernetes Config

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: predictive-analytics-meca0rw1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: predictive-analytics-meca0rw1
  template:
    metadata:
      labels:
        app: predictive-analytics-meca0rw1
    spec:
      containers:
      - name: predictive-analytics-meca0rw1
        image: predictive-analytics-meca0rw1:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
```

---
*Generated by AI Service Factory on 2025-08-15*
