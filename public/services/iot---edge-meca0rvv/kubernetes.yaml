apiVersion: apps/v1
kind: Deployment
metadata:
  name: iot---edge-meca0rvv
  labels:
    app: iot---edge-meca0rvv
spec:
  replicas: 3
  selector:
    matchLabels:
      app: iot---edge-meca0rvv
  template:
    metadata:
      labels:
        app: iot---edge-meca0rvv
    spec:
      containers:
      - name: iot---edge-meca0rvv
        image: iot---edge-meca0rvv:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: iot---edge-meca0rvv-service
spec:
  selector:
    app: iot---edge-meca0rvv
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer