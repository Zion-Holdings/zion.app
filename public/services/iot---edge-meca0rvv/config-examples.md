# Configuration Examples - Intelligent IoT & Edge Hub

## Basic Configuration

```json
{
  "service": {
    "name": "Intelligent IoT & Edge Hub",
    "version": "1.0.0",
    "port": 3000
  },
  "database": {
    "url": "postgresql://localhost:5432/iot---edge-meca0rvv",
    "pool": {
      "min": 2,
      "max": 10
    }
  },
  "api": {
    "rateLimit": 100,
    "timeout": 30000
  }
}
```

## Docker Compose

```yaml
version: '3.8'
services:
  iot---edge-meca0rvv:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: iot---edge-meca0rvv
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password

  redis:
    image: redis:7-alpine
```

## Kubernetes Config

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iot---edge-meca0rvv
spec:
  replicas: 3
  selector:
    matchLabels:
      app: iot---edge-meca0rvv
  template:
    metadata:
      labels:
        app: iot---edge-meca0rvv
    spec:
      containers:
      - name: iot---edge-meca0rvv
        image: iot---edge-meca0rvv:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
```

---
*Generated by AI Service Factory on 2025-08-15*
