# Configuration Examples - Next-Gen Real-time Analytics Framework

## Basic Configuration

```json
{
  "service": {
    "name": "Next-Gen Real-time Analytics Framework",
    "version": "1.0.0",
    "port": 3000
  },
  "database": {
    "url": "postgresql://localhost:5432/real-time-analytics-meca0x7e",
    "pool": {
      "min": 2,
      "max": 10
    }
  },
  "api": {
    "rateLimit": 100,
    "timeout": 30000
  }
}
```

## Docker Compose

```yaml
version: '3.8'
services:
  real-time-analytics-meca0x7e:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: real-time-analytics-meca0x7e
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password

  redis:
    image: redis:7-alpine
```

## Kubernetes Config

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: real-time-analytics-meca0x7e
spec:
  replicas: 3
  selector:
    matchLabels:
      app: real-time-analytics-meca0x7e
  template:
    metadata:
      labels:
        app: real-time-analytics-meca0x7e
    spec:
      containers:
      - name: real-time-analytics-meca0x7e
        image: real-time-analytics-meca0x7e:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
```

---
*Generated by AI Service Factory on 2025-08-15*
