# Configuration Examples - Smart Computer Vision System

## Basic Configuration

```json
{
  "service": {
    "name": "Smart Computer Vision System",
    "version": "1.0.0",
    "port": 3000
  },
  "database": {
    "url": "postgresql://localhost:5432/computer-vision-meca0x9x",
    "pool": {
      "min": 2,
      "max": 10
    }
  },
  "api": {
    "rateLimit": 100,
    "timeout": 30000
  }
}
```

## Docker Compose

```yaml
version: '3.8'
services:
  computer-vision-meca0x9x:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: computer-vision-meca0x9x
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password

  redis:
    image: redis:7-alpine
```

## Kubernetes Config

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: computer-vision-meca0x9x
spec:
  replicas: 3
  selector:
    matchLabels:
      app: computer-vision-meca0x9x
  template:
    metadata:
      labels:
        app: computer-vision-meca0x9x
    spec:
      containers:
      - name: computer-vision-meca0x9x
        image: computer-vision-meca0x9x:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
```

---
*Generated by AI Service Factory on 2025-08-15*
