# Configuration Examples - Intelligent Data Pipeline System

## Basic Configuration

```json
{
  "service": {
    "name": "Intelligent Data Pipeline System",
    "version": "1.0.0",
    "port": 3000
  },
  "database": {
    "url": "postgresql://localhost:5432/data-pipeline-meca0x7d",
    "pool": {
      "min": 2,
      "max": 10
    }
  },
  "api": {
    "rateLimit": 100,
    "timeout": 30000
  }
}
```

## Docker Compose

```yaml
version: '3.8'
services:
  data-pipeline-meca0x7d:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: data-pipeline-meca0x7d
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password

  redis:
    image: redis:7-alpine
```

## Kubernetes Config

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-pipeline-meca0x7d
spec:
  replicas: 3
  selector:
    matchLabels:
      app: data-pipeline-meca0x7d
  template:
    metadata:
      labels:
        app: data-pipeline-meca0x7d
    spec:
      containers:
      - name: data-pipeline-meca0x7d
        image: data-pipeline-meca0x7d:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
```

---
*Generated by AI Service Factory on 2025-08-15*
