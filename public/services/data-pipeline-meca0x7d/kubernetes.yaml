apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-pipeline-meca0x7d
  labels:
    app: data-pipeline-meca0x7d
spec:
  replicas: 3
  selector:
    matchLabels:
      app: data-pipeline-meca0x7d
  template:
    metadata:
      labels:
        app: data-pipeline-meca0x7d
    spec:
      containers:
      - name: data-pipeline-meca0x7d
        image: data-pipeline-meca0x7d:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: data-pipeline-meca0x7d-service
spec:
  selector:
    app: data-pipeline-meca0x7d
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer