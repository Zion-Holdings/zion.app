apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai---machine-learning-meca0rvw
  labels:
    app: ai---machine-learning-meca0rvw
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ai---machine-learning-meca0rvw
  template:
    metadata:
      labels:
        app: ai---machine-learning-meca0rvw
    spec:
      containers:
      - name: ai---machine-learning-meca0rvw
        image: ai---machine-learning-meca0rvw:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ai---machine-learning-meca0rvw-service
spec:
  selector:
    app: ai---machine-learning-meca0rvw
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer