# Configuration Examples - Intelligent Quantum Computing Platform

## Basic Configuration

```json
{
  "service": {
    "name": "Intelligent Quantum Computing Platform",
    "version": "1.0.0",
    "port": 3000
  },
  "database": {
    "url": "postgresql://localhost:5432/quantum-computing-mecav0lt",
    "pool": {
      "min": 2,
      "max": 10
    }
  },
  "api": {
    "rateLimit": 100,
    "timeout": 30000
  }
}
```

## Docker Compose

```yaml
version: '3.8'
services:
  quantum-computing-mecav0lt:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: quantum-computing-mecav0lt
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password

  redis:
    image: redis:7-alpine
```

## Kubernetes Config

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantum-computing-mecav0lt
spec:
  replicas: 3
  selector:
    matchLabels:
      app: quantum-computing-mecav0lt
  template:
    metadata:
      labels:
        app: quantum-computing-mecav0lt
    spec:
      containers:
      - name: quantum-computing-mecav0lt
        image: quantum-computing-mecav0lt:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
```

---
*Generated by AI Service Factory on 2025-08-15*
