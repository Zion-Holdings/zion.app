import Head from 'next/head';
import Link from 'next/link';

export default function ReportsIndex() {
  const reports: Array<{ href: string; title: string; desc: string }> = [
    { href: '/reports/seo', title: 'AI SEO Auditor', desc: 'Automated site SEO analysis' },
    { href: '/reports/ai-trends', title: 'AI Trends Radar', desc: 'Latest AI signals' },
    { href: '/reports/roadmap', title: 'Autonomous Roadmap', desc: 'Planned and ongoing work' },
    { href: '/newsroom', title: 'Newsroom', desc: 'Curated updates' },
  ];

  return (
    <div className="min-h-screen bg-slate-950 text-white">
      <Head>
        <title>Reports — Zion</title>
        <meta name="description" content="Index of public reports and dashboards." />
      </Head>
      <main className="mx-auto max-w-7xl px-6 py-12">
        <h1 className="text-4xl font-extrabold mb-2">Reports</h1>
        <p className="text-white/70 mb-6">Explore automated reports generated by our agents.</p>
        <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
          {reports.map((r) => (
            <Link key={r.href} href={r.href}>
              <a className="group relative overflow-hidden rounded-2xl border border-white/10 bg-gradient-to-br from-white/10 to-white/5 p-6 backdrop-blur-xl hover:border-cyan-400/30">
                <div className="pointer-events-none absolute -inset-px -z-10 bg-gradient-to-r from-fuchsia-500/0 via-cyan-400/10 to-fuchsia-500/0 opacity-0 blur-2xl transition-opacity group-hover:opacity-100" />
                <div className="text-base font-semibold">{r.title}</div>
                <div className="mt-1 text-sm text-white/75">{r.desc}</div>
                <div className="mt-3 inline-flex items-center gap-1 text-xs text-cyan-300/90">Open <span aria-hidden>→</span></div>
              </a>
            </Link>
          ))}
        </div>
      </main>
    </div>
  );
}


