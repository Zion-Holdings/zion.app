#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "📝 Preparing commit message..."

# Add timestamp to commit message if not present
if ! grep -q "\[$(date +%Y-%m-%d)\]" "$1"; then
  echo "" >> "$1"
  echo "[$(date +%Y-%m-%d %H:%M:%S)]" >> "$1"
fi

echo "✅ Commit message prepared"
