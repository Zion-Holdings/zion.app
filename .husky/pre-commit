#!/usr/bin/env sh
set -e

echo "🔍 Running pre-commit checks..."

# TypeScript type check
npm run type-check || {
  echo "❌ TypeScript type check failed. Please fix issues before committing."
  exit 1
}

# Optional: add other lightweight checks here

echo "✅ Pre-commit checks passed"
