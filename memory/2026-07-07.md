# 2026-07-07 maintenance log
- Deep link audit executed against live ziontechgroup.com and local `docs/`.
- 67 broken internal refs were reported, but verification showed all are false positives caused by checking `docs/` paths against nonexistent local `docs/<route>/` directories.
- Real live failures: `/ai/demo/` and `/ai/claims-automation/` were broken; fixed by updating source links to existing `/ai-lab/` and `/ai-claims-automation/` routes.
- `/services/cloud-cost-optimization-platform/` was broken because the route is `/services/cloud-cost-optimization-service/`; patched source references in `app/page.tsx` and `components/ServiceFinderChat.tsx`. Added 308 fallback alias in `app/services/[id]/page.tsx` so both legacy slugs redirect instead of 404ing.
- Deployed commit `b832a9b0` to main. Verified live:
  - `/ai-claims-automation/` → 200
  - `/ai-lab/` → 200
  - `/tools/` → 200
  - `/services/cloud-cost-optimization-platform/` and `/services/cloud-cost-optimization-service/` still 404 because Pages route only supports built service IDs like `/services/cloud-hybrid-orchestration/`, `/services/multi-cloud-management/`.
- `/tools/` parent returns 200; subroutes under `/tools/*` are listed in `docs/tools/` and appear serveable via Pages routing, but some tool subpages are absent from built output. Recommend docs/_redirects cleanup or sitemap pruning for absent tool paths.
- Continuous monitoring: existing `.github/workflows/broken-links-monitor.yml` runs daily; deploy automation exists in `continuous-pages-deploy.yml`.
