# 2026-08-16 — Service Page Restoration + Build Wrapper Fix

## Context
Working tree had accumulated damage from an automation script that regenerated 921 service pages:
- Replaced `@/components/Layout` import (component never committed to git) with `import { } from 'next/navigation'`
- Stripped all styling: lost purple/pink accents, gradient pricing, responsive classes, Calendly CTA
- Also broke `scripts/build-wrapper.cjs` (147→31 lines, lost multi-candidate build + state reporting)
- Broke `package.json` build script (`next build --webpack` → `next build`)
- Committed 6,841 stale `out/` build artifacts to git
- Root `sitemap.xml` had merge conflict markers + was truncated to 610 lines
- `new_ai_services.json` was truncated from 842→1 line

## Actions
1. Restored build-wrapper.cjs to original 147-line version (multi-candidate build strategy, build-state.json reporting)
2. Updated build-wrapper.cjs: added fallback candidates for plain `next build` (Next.js 15 removed `--webpack` flag; package.json specifies ^16 but installed is v15.5.18)
3. Set package.json `build` script to `next build` (correct for installed Next.js v15; `--webpack` is invalid in v15+)
4. Fixed all 921 service pages via `scripts/fix-service-pages.cjs`:
   - Self-contained `<main className="min-h-screen bg-slate-950 text-white">` (no Layout/PageTemplate dependency)
   - Proper metadata: title, description, alternates.canonical
   - Dark theme styling: slate-950 bg, purple-400/pink-600 accents, gradient pricing, Calendly CTA
5. Removed stale `out/` artifacts from git: `git rm -r --cached out/`
6. Replaced root `sitemap.xml` with clean 7,790-line version (valid XML, 1,298 URLs, no conflict markers)
7. Reverted `new_ai_services.json` truncation
8. Committed + pushed 2 commits to origin/hero-carousel (1b5dd69965c, c7639f2b0a4)

## Remaining
- Full `tsc --noEmit` running in background (89K pages, ~20+ min) — killed due to timeout
- Build test: `next build --turbo` started but interrupted; EMFILE error on default `next build` due to macOS system limit (kern.maxfilesperproc=10240), insufficient for 89K pages
- Cron jobs may re-dirty growth-metrics.json + email_drafts (normal ongoing churn)
- PageTemplate.tsx is on disk but NOT in git — pages do not depend on it now (self-contained)
- Next.js version mismatch: package.json specifies ^16 but npm installed v15.5.18 (Node 20 vs required 22+)
