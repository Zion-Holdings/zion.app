# 2026-08-16 — Service Page Restoration + Build Wrapper Fix

## Context
Working tree had accumulated damage from automation scripts across multiple commits:
- 921 service pages had `@/components/Layout` import (component never committed to git) → bare `<main>`
- 7,482 service pages had `@/components/Layout` import → stripped styling, no metadata
- 20,849 service pages had empty `import { } from 'next/navigation'` dead imports
- 6 pages had "mo/month" pricing typo
- 9 blog pages had broken JSX: `dangerouslySetInnerHTML={__html:` (missing object braces)
- build-wrapper.cjs stripped 147→31 lines, package.json build script broken
- 6,841 stale `out/` artifacts committed to git
- Root `sitemap.xml` had merge conflict markers + truncated
- `new_ai_services.json` was truncated from 842→1 line
- tsconfig.json committed with invalid `dynamicParams` compiler option (Next.js config option, not tsc)

## Actions
1. Fixed 7,482 pages with Layout import → self-contained dark theme styling + canonical metadata (scripts/fix-layout-imports.cjs)
2. Removed 20,849 empty `import { } from 'next/navigation'` dead imports (sed parallel)
3. Fixed 6 "mo/month" → "/mo" pricing typos (sed)
4. Fixed 9 blog pages: `dangerouslySetInnerHTML={__html:` → `dangerouslySetInnerHTML={{__html: ...}}` (double braces)
5. Restored build-wrapper.cjs with fallback candidates for Next.js 15/16
6. Set package.json build to `next build` (Next.js v15.5.18 installed locally; `--webpack` for v16 in CI)
7. Removed stale `out/` from git tracking
8. Fixed root sitemap.xml (conflict markers → clean 7,790 lines)
9. Reverted new_ai_services.json truncation
10. Added tsconfig.json excludes: `out`, `app/services/*/` to prevent OOM
11. Removed invalid `dynamicParams` from tsconfig.json compilerOptions (committed on hero-carousel via upstream commit 3382cf1e172)
12. Removed tracked tsconfig.tsbuildinfo, added `*.tsbuildinfo` to .gitignore

## Commits (all pushed)
### hero-carousel:
- `1b5dd69965c` — first batch: 921 page fixes + build wrapper + sitemap + out/ cleanup
- `c7639f2b0a4` — build script for Next.js 15 compatibility
- `09760bd041b` — tsconfig excludes + remove tracked build artifact
- `eb85a129ca8` — remove 20,849 dead imports + fix mo/month typos
- `cd69ef39c26` — MEMORY.md update
- `8d6d9107254` — fix JSX syntax in 9 blog pages (dangerouslySetInnerHTML)

### main:
- `e1296f82173` — same blog page JSX fix (cherry-pick target)

## Verified
- 0 pages with Layout import in app/services/
- 0 pages with empty next/navigation import
- 0 pages with bare <main> (not <main className>)
- 0 pages with "mo/month" typo
- 0 pages missing canonical metadata
- 0 pages missing metadata export
- 0 blog pages with broken dangerouslySetInnerHTML syntax
- 20 sample pages pass TypeScript transpileModule type-check (0 errors)
- tsconfig.json has no invalid compiler options
- tsc OOMs locally (89K pages, 4GB heap) but tsconfig now excludes service stubs

## Remaining Limitations
- Full `tsc --noEmit` OOMs on local 89K+ page repo (4GB heap, ~20+ min) — CI uses 8GB
- `next build` EMFILE on macOS (system limit kern.maxfilesperproc=10240 < 89K pages) — CI has higher limits
- Next.js version mismatch: package.json specifies ^16 but installed is v15.5.18 (Node 20 vs required 22+)
- Blog pages and slug-named service pages still use $$ pricing (double dollar) vs $ in UUID pages — both valid conventions
