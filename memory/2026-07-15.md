# Daily Notes - 2026-07-15

## Cron: outreach processing
- Ran `lead-crm/send_outreach_batch.py` against `lead-crm/outreach_ready_canonical.json`.
- `ZTG_SEND_ALLOWED` unset in this cron environment → batch returned `SEND_DISABLED` with `send_count: 0`.
- Ran `lead-crm/_cron_send_disabled_analysis.py` for send-disabled analysis/improvement.
- Analysis found 25 recipient records, 15 duplicate-domain contacts across 3 domains (`dekra.com`, `hcl.com`, `solyssey.com`), max domain count 9, 25 generic subjects, 0 missing names.
- No outreach improvement body changes were applied because all records already had body content; all records were marked send-disabled improved in canonical.
- `lead-crm/miner_health.json` updated with send-disabled stats; `send_disabled_run_count` is now 1.
- `lead-crm/outreach_send_disabled_analysis.jsonl` appended with this run.

## Cron: hot-follow-up email processing
- Searched Gmail for `label:"!!!hot-follow-up"` across all folders.
- Result: 0 threads found with the `!!!hot-follow-up` label.
- No drafts created, no ledger entries added.
- Next check needed only if new `!!!hot-follow-up` emails arrive.
