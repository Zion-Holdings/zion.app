# 2026-08-13

## Session Summary: Workflow Integrity Fix + Git Repo Resolution

### Task
Address GitHub Actions workflow failures (>=500 blind failures) identified in HEARTBEAT.md, resolve git repo confusion, and clean up disk space.

### Key Accomplishments
- **Fixed `on:` → `"on":` YAML quoting** across all 393 GitHub Actions workflow files
- **Root cause identified**: commit `a6fe35a00b7` ("bulk update 393 workflows") reverted the `9ea291ff7f1` quoting fix, leaving all workflows with unquoted `on:` which YAML 1.1 parses as boolean `True`, causing GHA to not recognize the workflow triggers
- **Fixed git repo confusion**: project repo at `/Users/klebergarciaalcatrao/zion-support.github.io` has its own `.git` directory; terminal `workdir` parameter sometimes resolves to the home directory's `.git` instead — always use `git -C` with absolute path
- **Cleaned 50GB `.hermes` cache** that was filling the disk (116MB available → 685MB after cleanup)
- **Killed 12+ stuck git processes** from overlapping background operations (stale index.lock, parallel LFS post-commit hooks, zombie ls-files)
- **Committed + pushed** workflow fix to `origin/hero-carousel` (commit `f324897c5d2`)
- **Confirmed `gh auth` working** for Zion-support account

### Working tree state
- Local `hero-carousel` synced with `origin/hero-carousel` (0 behind, 0 ahead)
- `servicesData.json`, `sitemap.xml`, `github-workflow-integrity-audit-latest.json` continuously re-dirtied by active growth engine cron jobs (expected — next sync cycle will commit them)

### Post-mortem
- What went well: The automated Python fix script handled all 393 files in one pass; `git -C` resolved the repo confusion; disk cleanup freed 50GB instantly
- What could be better: Terminal `workdir` doesn't always isolate git commands; need to always use explicit `-C` flag. Also need to kill stale background processes before starting git operations

### Rules extracted
- `[CONDITION] GitHub Actions workflow has unquoted on: key → [ACTION] Quote as "on": to prevent YAML boolean parsing`
- `[CONDITION] Terminal workdir doesn't resolve to project git repo → [ACTION] Always use git -C /absolute/repo/path for git operations`
- `[CONDITION] Disk at 100% from .hermes cache → [ACTION] Clean .hermes/cache/terminal-output/ and .hermes/audio_cache/ to free space`
- `[CONDITION] Stuck git processes from background jobs → [ACTION] Kill stale processes (git commit, git lfs, ls-files) and remove .git/index.lock before retrying`

### Gaps identified
- `[GAP: .hermes cache retention policy]` — Need automatic cleanup of .hermes/cache to prevent disk filling
- `[GAP: workflow quoting fix durability]` — Commit a6fe35a00b7 reverted 9ea291ff7f1 quoting fix; need a pre-commit hook or CI guard to prevent regression
- `[GAP: background process cleanup]` — Need a watchdog to detect and kill stale git processes before starting new operations
