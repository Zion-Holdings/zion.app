# 2026-08-08

## Git recovery
- `.git/index` corrupted (smaller than expected). Fixed by `rm -f .git/index` + `git read-tree HEAD`.
- Stale `.git/rebase-merge` directory blocked rebase. Removed it.
- Repo is extremely large (80,751 files) — git operations take 50-120s.
- Multiple agents pushing to `main` causes frequent rebase conflicts.

## Broken URL fix
- Live site 404s: `/free-consultation/`, `/industries/financial-services/`
- Root cause: redirect target files existed at repo root, not in `public/`. GitHub Actions deploy workflow copies `public/` to artifact root, but root-level files were not included.
- Fix: copied `industries/financial-services/`, `industries/insurance/`, `industries/legal/` index.html into `public/industries/`. Upgraded `public/free-consultation/index.html` from stub (1380 bytes) to full page (5555 bytes).
- Commit `eb8f1028fb7` pushed to origin/main. CI workflow (`gh-pages.yml`) deploys automatically.

## Build
- Next.js build with Turbopack takes >15 min for 1000+ pages. Running in background with NODE_OPTIONS=--max-old-space-size=8192.
- `npx tsc --noEmit` OOMs even with 4GB — too many files for direct tsc.
