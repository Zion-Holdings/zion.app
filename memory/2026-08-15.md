# Memory for 2026-08-15

## Tasks
- [x] Resume from expired session — check git status and working tree
- [x] Fix unquoted `on:` YAML keys in GitHub Actions workflows
- [x] Verify servicesData.json integrity after cron modification
- [x] Commit + push to hero-carousel

## Repo State
- Branch: `hero-carousel`
- HEAD (local): `84abec0a645`
- HEAD (origin/hero-carousel): was `8fd344c6f59` before push, now `84abec0a645`
- Working tree had unstaged changes from cron jobs: servicesData.json, sitemap.xml, public/sitemap.xml

## What Was Done
1. **Workflow YAML integrity fix**: Found 406 workflows with unquoted `on:` key (YAML 1.1 parses as boolean True). Fixed 401 active workflow files (5 remaining are `.disabled` — ignored). Verified all 401 parse with `yaml.safe_load` — 0 errors.
2. **servicesData.json integrity**: 
   - File grew from 121MB→133MB (LFS pointer updated) due to active growth engine cron jobs
   - 12,500 entries missing `title` field — backfilled from `name` (always identical in this dataset)
   - 0 missing IDs, 0 duplicate IDs, 0 duplicate hrefs
   - clean-services.cjs pre-commit hook ran on commit — verified 81,134 entries all valid
3. **Accidentally reverted package.json**: `next build --webpack` was changed back to `next build` by a cron job diff — restored with `git checkout -- package.json`

## Key Context
- servicesData.json is actively modified by growth engine cron jobs (~1000 services/minute)
- Git pushes of 133MB LFS file take >5 min — must use background=true
- Multiple `.disabled` workflow files exist in `.github/workflows/` — leave them alone
- `out/` directory is gitignored but has 6838 stale tracked files from old commits — deletions are noise, don't stage
- Commit `84abec0a645`: fix: workflow on: YAML key quoting + backfill title field in 12500 services

## Next Up
- Deploy to gh-pages via GitHub Actions (trigger gh-pages.yml workflow)
- Consider cleaning stale `out/` tracked files from git index
