# 2026-07-16 log
- cron outreach run #1: `ZTG_SEND_ALLOWED` unset → `send_outreach_batch.py` returned `SEND_DISABLED`; executed `lead-crm/_cron_send_disabled_analysis.py`.
- cron outreach run #2 (current): analyzed 25 recipients, improved 25 generic records with template bodies, flagged 15 duplicate-domain contacts across `hcl.com`/`solyssey.com`/`dekra.com`. Updated `outreach_ready_canonical.json` and `miner_health.json`, appended `outreach_send_disabled_analysis.jsonl`.
- No sends performed; send policy honored.
- Gap: unable to locate `docs/calendar/systems/support schedule` or any "01_crm" reference for the reported 03:22 cron health issue.
- cron outreach run #3: `ZTG_SEND_ALLOWED` unset → `send_outreach_batch.py` returned `SEND_DISABLED`; executed `lead-crm/_cron_send_disabled_analysis.py`: analyzed 25 recipients, improved 0 already-personalized generic records, flagged 15 duplicate-domain contacts across `hcl.com`/`solyssey.com`/`dekra.com`. Updated `outreach_ready_canonical.json` and `lead-crm/miner_health.json`, appended `lead-crm/outreach_send_disabled_analysis.jsonl`. No sends performed; send policy honored.
- cron outreach run #4 (current): `ZTG_SEND_ALLOWED` unset → `send_outreach_batch.py` returned `SEND_DISABLED`; executed `lead-crm/_cron_send_disabled_analysis.py`: analyzed 25 recipients, improved 25 generic records with template bodies, flagged 15 duplicate-domain contacts across `hcl.com`/`solyssey.com`/`dekra.com`. Updated `outreach_ready_canonical.json` and `lead-crm/miner_health.json`, appended `lead-crm/outreach_send_disabled_analysis.jsonl`. No sends performed; send policy honored.
- cron autopilot run (cron/autonomous-site-outreach-autopilot): crawl all key paths HTTP 200; no broken links; no redirects added. Homepage CTAs present (Start a project, See services, Free tools); design unchanged.
- Gmail scan for "!!!hot-follow-up" returned 1 thread; excluded by prior dedup (solyssey). threads_checked=1, threads_sent=0.
- Quality: npm run test:ci passed (13/13); npm run type-check failed pre-existing TS errors in servicesData.ts; npm run lint:check crashed due Node 22 + @typescript-eslint incompatibility; npm run build failed with duplicate allServices declaration in servicesData.ts.
- Build blocked (Node 22 vs .nvmrc=20 + pre-existing TS/webpack error).
- Commit/push: added crawl artifacts and probe script; pushed 05d399dc to origin/main.
- cron outreach run #5: `ZTG_SEND_ALLOWED` unset → `send_outreach_batch.py` returned `SEND_DISABLED`; executed `lead-crm/_cron_send_disabled_analysis.py`: analyzed 25 recipients, improved 25 generic records with template bodies, flagged 15 duplicate-domain contacts across `hcl.com`/`solyssey.com`/`dekra.com`. Updated `outreach_ready_canonical.json` and `miner_health.json`, appended `outreach_send_disabled_analysis.jsonl`. No sends performed; send policy honored.
