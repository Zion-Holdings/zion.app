# 2026-07-15 autopilot crawl report

## Live probe summary
- Live site responded on HTTPS with valid certificate.
- Core paths returned HTTP 200: `/`, `/services/`, `/tools/`, `/contact/`, `/pricing/`, `/solutions/`, `/blog/`, `/about/`.
- `/sitemap.xml` and `/robots.txt` returned 200.

## Confirmed broken candidates returning 404
- `/free-tools` and `/free-tools/`
- `/tools/roi-calculator` and `/tools/roi-calculator/`
- `/tools/analytics` and `/tools/analytics/`
- `/solutions/ai-consulting` and `/solutions/ai-consulting/`
- `/pricing/ai-services` and `/pricing/ai-services/`
- `/contact/thank-you` and `/contact/thank-you/`
- `/proposal-generator` and `/proposal-generator/`
- `/client-portal` and `/client-portal/`
- `/roadmap` and `/roadmap/`

## Same-page local source paths
Local cloned source exists for: `/tools/roi-calculator/`, `/tools/analytics/`, `/proposal-generator/`, `/client-portal/`, `/free-ai-tools/`
Local cloned source is missing for: `/solutions/ai-consulting/`, `/pricing/ai-services/`, `/contact/thank-you/`, `/free-tools/`

## Outreach scan
No uncontacted `!!!hot-follow-up` external client threads were found in the live Gmail scan. No CEO reply was sent.

## CTA decision
Homepage already exposes strong conversion paths: `Start a project`, `Book a call`, `Google Meet`, `Contact form`, and a "Ready to move?" section. No CTA/section additions were made.
