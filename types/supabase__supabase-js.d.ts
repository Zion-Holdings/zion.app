declare module '@supabase/supabase-js' {
  export interface SupabaseClient {
    // Supabase client interface for type compatibility
  }
  export interface User {
    // User interface for type compatibility
  }
  export interface Session {
    // Session interface for type compatibility
  }
  export interface AuthChangeEvent {
    // Auth change event interface for type compatibility
  }
}
